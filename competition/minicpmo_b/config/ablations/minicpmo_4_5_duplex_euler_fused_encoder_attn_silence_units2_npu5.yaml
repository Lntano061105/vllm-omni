# Combine the low-TTFT fused Token2Wav encoder attention with two native
# one-second silence units per continuation append.  The latter reduced SPEAK
# RTF by about 10.8% alone; this candidate tests whether the fused encoder
# recovers its TTFT/TTFP regression.
base_config: minicpmo_4_5_duplex_euler_fused_encoder_attn_npu5.yaml
pipeline: minicpmo_4_5

duplex_session:
  native_silence_continuation_delay_ms: 0
  native_silence_continuation_units_per_append: 2
